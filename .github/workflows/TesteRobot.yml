name: Run Robot Framework Tests with Allure

on:
  # push:
  #   branches: [ master ]
  # pull_request:
  #   branches: [ master ]
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install robotframework==7.3.2
          pip install robotframework-seleniumlibrary
          pip install robotframework-requests
          pip install allure-robotframework

      # 👇 Corrigido: Agora o Robot Framework gera os resultados do Allure diretamente
      - name: Run Robot Framework tests
        # Usamos 'continue-on-error: true' para que as próximas etapas de upload rodem mesmo se os testes falharem
        continue-on-error: false
        run: |
          mkdir -p allure-results # Garante que a pasta exista antes de rodar os testes
          python -m robot --listener allure_robotframework scripts/
          


      # 👇 Armazena resultados do Allure para visualização posterior
      - name: Upload Allure Results
        uses: actions/upload-artifact@v4
        if: always() # Isso garante que o upload tente ocorrer sempre
        with:
          name: allure-results
          path: allure-results/
